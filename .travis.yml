language: go

go:
  - 1.11.x

before_install:
  # install ffmpeg
  - sudo apt-get -qq update && sudo apt-get install -y ffmpeg

install:
  - go get -t ./...

script:
  - go vet ./...
  - go test ./...
  - go build
